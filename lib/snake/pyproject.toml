[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["snake"]

[project]
name = "snake"
version = "0.0.1"
dependencies = [
  "gymnasium",
]
